services:
  r_analytics:
    base_url: ${R_ANALYTICS_URL:-http://r-analytics:8000}
    health_path: /health
    timeout_s: 3
    retries: 2

  postgres:
    host: postgres
    port: 5432
    dbname: iassist
    user: admin
    password: secret   # will be overridden by env in real deployments
