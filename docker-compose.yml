services:
  # ==============================
  # 🧠 AI Core (Python FastAPI)
  # ==============================
  ai-core:
    build:
      context: ./docker/ai-core
      dockerfile: Dockerfile
    container_name: ai-core
    ports:
      - "5000:5000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-test_key_placeholder}
      - R_ANALYTICS_URL=http://r-analytics:8000
      # Optional: point to private orchestrator if mounted by override file
      - IA_INTELLIGENCE_ORCHESTRATOR_PATH=/app/intelligence/orchestrator.py
    networks:
      - iassist-net

  # ==============================
  # 📊 R Analytics (Plumber API)
  # ==============================
  r-analytics:
    build:
      context: ./docker/r-analytics
      dockerfile: Dockerfile
    container_name: r-analytics
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    networks:
      - iassist-net
    depends_on:
      - postgres

  # ==============================
  # ⚙️ API Gateway (.NET 9)
  # ==============================
  api-gateway:
    build:
      context: ./docker/api-gateway
      dockerfile: Dockerfile
    container_name: api-gateway
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - ./.env
    depends_on:
      - ai-core
      - r-analytics
      - postgres
    networks:
      - iassist-net

  # ==============================
  # 🧮 ETL / Pipelines
  # ==============================
  pipelines:
    build:
      context: ./docker/pipelines
      dockerfile: Dockerfile
    container_name: pipelines
    restart: on-failure
    command: ["python", "etl_job.py"]
    env_file:
      - ./.env
    depends_on:
      - postgres
    networks:
      - iassist-net

  # ==============================
  # 🗄️ Postgres Database
  # ==============================
  postgres:
    image: postgres:16
    container_name: postgres
    restart: always
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./docker/db/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - iassist-net

  # ==============================
  # 🧭 Airflow (Automation & Scheduling)
  # ==============================
  airflow:
    build:
      context: ./docker/airflow
      dockerfile: Dockerfile
    container_name: airflow
    restart: always
    env_file:
      - ./.env
    environment:
      - AIRFLOW__CORE__EXECUTOR=SequentialExecutor
      - AIRFLOW__CORE__FERNET_KEY=${AIRFLOW__CORE__FERNET_KEY}
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
    ports:
      - "8081:8080"
    # Public repo ships with stub DAGs so Airflow runs standalone
    volumes:
      - ./docker/airflow/dags:/opt/airflow/dags
    depends_on:
      - postgres
    networks:
      - iassist-net

networks:
  iassist-net:

volumes:
  db_data:
  